<p align="center">
  <strong>Русский</strong> · <a href="docs/en/README.md">English</a> · <a href="docs/zh-CN/README.md">简体中文</a>
</p>

<p align="center">
  <img src="assets/logo.png" alt="Hydrat Logo" width="680" />
</p>
<p align="center">
  <a href="https://golang.org"><img src="https://img.shields.io/github/go-mod/go-version/only-hydrat/hydrat?style=flat-square" alt="Go Version" /></a>
  <a href="LICENSE"><img src="https://img.shields.io/badge/License-MIT-blue.svg?style=flat-square" alt="License: MIT" /></a>
  <a href="https://github.com/only-hydrat/hydrat/actions/workflows/ci.yml"><img src="https://img.shields.io/github/actions/workflow/status/only-hydrat/hydrat/ci.yml?branch=main&label=CI&style=flat-square" alt="CI Status" /></a>
  <a href="docker-compose.yml"><img src="https://img.shields.io/badge/Docker-Compose-2496ED?logo=docker&logoColor=white&style=flat-square" alt="Docker" /></a>
</p>


<p align="center">
  <strong>Высоконадежный интеллектуальный VPN-шлюз на базе WireGuard, Xray и Tor</strong><br>
  Автоматический отбор лучших VLESS и Tor-прокси, фоновая квалификация серверов, изоляция сбоев и бесшовное переключение без обрыва WireGuard-сессии.
</p>

<p align="center">
  <a href="#в-чём-проблема-существующих-решений">Почему Hydrat?</a> •
  <a href="#ключевые-преимущества">Преимущества</a> •
  <a href="#интерфейс">Интерфейс</a> •
  <a href="#быстрый-запуск">Быстрый запуск</a> •
  <a href="#архитектура-и-инженерные-решения">Архитектура</a> •
  <a href="#эксплуатация-и-мониторинг">Эксплуатация</a>
</p>

---

## В чём проблема существующих решений?

| Решение | Ограничения и проблемы |
|---|---|
| **Vanilla WireGuard / OpenVPN** | Протоколы мгновенно распознаются и блокируются ТСПУ / DPI на уровне провайдеров. |
| **Amnezia / Outline** | Привязаны к одному фиксированному IP/серверу. При блокировке адреса или падении хостинга связь пропадает целиком; требуется ручная перенастройка. |
| **Локальные клиенты (v2rayN, Happ, Hiddify, sing-box)** | • Быстро разряжают аккумулятор смартфона постоянными фоновыми замерами.<br>• Требуют установки сторонних приложений на каждое устройство.<br>• Сложно или невозможно настроить на домашних роутерах, Apple TV и Smart TV.<br>• В подписках тысячи битых/заспамленных ссылок — при падении сервера приходится вручную открывать приложение и перебирать узлы. |

### Решение Hydrat

**Hydrat объединяет простоту WireGuard на клиенте с мощью Xray и Tor на выходе.**

Вы подключаете телефон, ноутбук или домашний роутер к Hydrat через **стандартный, нативный WireGuard**. Шлюз работает на вашем VPS, загружает ваши подписки (тысячи VLESS- и Tor-серверов), непрерывно тестирует их в изолированных фоновых средах и на лету заворачивает ваш трафик в самый быстрый и стабильный прокси-канал.

**Главное преимущество: переключение маршрута без разрыва WireGuard-туннеля.** При отказе текущего прокси Hydrat переводит новые потоки на резервный маршрут на шлюзе. Уже установленный TCP/QUIC поток может потребовать переподключения, если сменился egress или публичный IP.

---

## Ключевые преимущества

### 1. Нативный WireGuard на устройствах
Подключение через официальные клиенты WireGuard для iOS, Android, macOS, Windows, Linux, а также роутеры Keenetic, OpenWrt, Mikrotik. Никаких сложных китайских приложений, минимальное энергопотребление батареи смартфона.

### 2. Приоритет VLESS над Tor по умолчанию
Обычное назначение предпочитает пригодный VLESS для TCP. Для VLESS primary резерв предпочитает VLESS из другого домена отказа, но может быть прогретым Tor, если это единственный безопасный по домену вариант. При аварийной замене пригодные прогретые VLESS и Tor сравниваются по домену отказа, нагрузке и score; UDP остаётся только на VLESS.

### 3. Согласованное назначение TCP и UDP
Если есть UDP-qualified VLESS, scheduler по возможности co-assign назначает его и для TCP, и для UDP. TCP и UDP могут использовать разные маршруты: TCP допускает Tor, а UDP — только UDP-qualified VLESS.

### 4. Автоматический турнирный отбор (Tournament Engine)
Шлюз способен хранить до 10 000 кандидатов из публичных и приватных подписок. Фоновый контур прогоняет серверы через двухэтапный турнир:
- **Fast Probe:** быстрая проверка доступности порта и handshake (тайм-аут 6 сек).
- **Full Probe:** полная проверка сквозной передачи данных и обязательных сервисных гейтов (тайм-аут 20 сек).
- Серверы получают динамический балл (`Score`) на основе задержки, скорости и стабильности. 3 ошибки подряд отправляют плохой узел в бан на 5 часов.

### 5. Изоляция доменов отказа (Failure Domains)
Если 50 конфигураций в подписке ведут на один и тот же сервер или CDN-фронтинг, падение этого сервера не должно считаться «отказом 50 разных узлов». Hydrat автоматически группирует серверы по доменам отказа и гарантирует, что в качестве резерва назначается узел из **другого, независимого домена**.

### 6. Контроль качества обслуживания (QoE) и настраиваемые Service Gates
Активный мониторинг непрерывно оценивает реальный пользовательский опыт:
- Измерение реального TTFB и скорости загрузкой контрольного блока 64 КиБ;
- По умолчанию проверяется доступность ключевых сервисов: YouTube, Instagram, Telegram Web/MTProto, ChatGPT и OpenAI API;
- **Настраиваемые эндпоинты и кастомные гейты:** в `config.yml` (секция `probes.gate_endpoints` и `probes.custom_gates`) можно переопределять адреса проверок и добавлять собственные API и сайты с заданными допустимыми HTTP-статусами;
- Валидация работы QUIC / HTTP/3 через SOCKS5 UDP;
- Защита от флаппинга (гистерезис): маршрут не переключается от случайного минутного колебания пинга, переключение на альтернативу требует подтверждённого преимущества по скорости (минимум на 30%).

### 7. Умная прямая маршрутизация (Smart Direct Routing & Geo-Data)
- **Прямой доступ к отечественным сервисам:** трафик к доменным суффиксам `.ru`, `.su`, `.рф` (`.xn--p1ai`), а также к российским сервисам в международных зонах (`thecode.media`, `habr.com` и настраиваемым `direct_domains`) направляется напрямую без проксирования. Это исключает разрывы соединений и блокировки зарубежных датацентровых IP системами защиты (Qrator, DDoS-Guard, Selectel, Cloudflare).
- **Интеграция с актуальными базами GeoIP и GeoSite:** шлюз поддерживает автоматическую загрузку и периодическое фоновое обновление датасетов [runetfreedom/russia-blocked-geosite](https://github.com/runetfreedom/russia-blocked-geosite) и [runetfreedom/russia-blocked-geoip](https://github.com/runetfreedom/russia-blocked-geoip). Российские ресурсы (`geosite:category-ru`, `geoip:ru`) маршрутизируются напрямую, а заблокированные ресурсы (`geosite:russia-blocked`, `geoip:russia-blocked`) гарантированно направляются через рабочие VLESS/Tor-прокси.
- **Защита Fail-Closed:** весь остальной недоверенный трафик строго маршрутизируется через прокси либо блокируется при отказе, исключая утечки исходного IP.
- **Защита от утечки российского трафика (Russian Egress Protection):** параметр `disallow_ru_egress: true` исключает серверы выхода, находящиеся на территории РФ, из квалификации. Это защищает чувствительный трафик от прохождения через узлы, подверженные государственной цензуре и блокировкам.
---

## Интерфейс

Веб-панель управления доступна внутри VPN по адресу `http://10.44.0.1/`. На хосте Compose по умолчанию публикует её только на `127.0.0.1:8088`; для внешнего доступа используйте WireGuard или HTTPS reverse proxy с аутентификацией. Не публикуйте панель напрямую как `0.0.0.0:8088`.

### 1. Главная панель (Обзор)
Статистика активных рабочих серверов VLESS, доступных доменов отказа, готовых Tor-профилей и состояния конвейера отбора:
<p align="center">
  <img src="assets/dashboard.png" alt="Панель обзора Hydrat" width="900" />
</p>

### 2. Управление клиентами
Создание профилей пользователей в один клик, отображение назначенных TCP/UDP маршрутов, генерация конфигурационных файлов и QR-кодов для мобильных устройств:
<p align="center">
  <img src="assets/clients.png" alt="Управление клиентами WireGuard" width="900" />
</p>

### 3. Источники и подписки
Добавление ссылок на подписки (VLESS, Tor, Happ, v2rayN, Clash YAML). Предварительный просмотр валидных кандидатов, ручное или автоматическое обновление:
<p align="center">
  <img src="assets/sources.png" alt="Управление источниками" width="900" />
</p>

### 4. Турнирная таблица кандидатов
Просмотр результатов тестирования серверов: баллы качества (Score), серии успешных проверок (Streak), статус QoE, TTFB и скорость:
<p align="center">
  <img src="assets/candidates.png" alt="Кандидаты и турнирная таблица" width="900" />
</p>

---

## Быстрый запуск

Деплой максимально упрощён и не требует ручных манипуляций или вспомогательных скриптов.

### Системные требования

- Сервер с Linux (Ubuntu 22.04 / 24.04, Debian 12 и др.);
- Установленные Docker Engine и Docker Compose (Compose v2);
- Доступ к устройству `/dev/net/tun` (доступно по умолчанию на KVM/Dedicated серверах);
- Открытый входящий порт `51820/udp`;
- Рекомендуемые ресурсы: от 1–2 vCPU и от 1 ГБ RAM.

---

### Шаг 1. Клонируйте репозиторий

```bash
git clone https://github.com/only-hydrat/hydrat.git
cd hydrat
```

### Шаг 2. Настройте файл окружения `.env`

Создайте `.env` из примера:

```bash
cp .env.example .env
```

Откройте `.env` в любом текстовом редакторе и укажите:

```dotenv
# Публичный IP или домен вашего сервера и UDP-порт WireGuard
WIREGUARD_ENDPOINT=203.0.113.10:51820

# Оставьте localhost; для внешнего доступа используйте WireGuard или HTTPS proxy
PORTAL_BIND_ADDRESS=127.0.0.1

# Порт веб-панели управления на хосте (по умолчанию 8088)
PORTAL_PORT=8088

# Пароль для входа в панель управления
HYDRAT_ADMIN_PASSWORD=придумайте_сложный_пароль
```

> **Примечание:** если ваши подписки требуют верификацию по аппаратному ключу Happ, раскомментируйте и укажите `HYDRAT_HWID`.

### Шаг 3. Запустите Hydrat

По умолчанию Hydrat загружает и запускает официальный публичный образ `ghcr.io/only-hydrat/hydrat:1.0.3`:

```bash
docker compose up -d
```

> **Примечание:** образ и версию можно переопределить переменной `HYDRAT_IMAGE` в `.env` (например, `HYDRAT_IMAGE=ghcr.io/only-hydrat/hydrat:1.0.3`).

### Сборка из исходного кода (для разработки)

Если вы вносите изменения в исходный код, используйте `docker-compose.dev.yml`:

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
```

Команда собирает один локальный образ `hydrat:dev` для обоих сервисов.

**Готово!** Сервис автоматически:
- Сгенерирует серверный приватный ключ WireGuard в `data/wireguard/wg0.conf` (права `0600`);
- Создаст SQLite и ключ `data/controller/secrets/master.key` (права `0600`) для field-level AES-GCM защиты чувствительных payload; это не шифрование всей БД и не защита от host-root;
- Применит правила маршрутизации nftables и запустит демоны Xray и Tor.

---

## Настройка подключения за 2 минуты

1. **Вход в панель управления:**
   До создания первого WireGuard-клиента откройте SSH-туннель с локального компьютера:
   ```bash
   ssh -N -L 8088:127.0.0.1:8088 <user>@<IP-вашего-сервера>
   ```
   Не закрывая SSH-сессию, откройте `http://127.0.0.1:8088/` в браузере. После создания клиента используйте `http://10.44.0.1/` из WireGuard либо HTTPS reverse proxy. Браузер отправляет пароль один раз, хранит Bearer-сессию только в памяти, а logout или restart контроллера её завершает.
2. **Добавление подписки:**
   Перейдите в раздел **«Источники»**, нажмите **«Добавить источники»**, вставьте URL подписки (VLESS или Tor) и нажмите **«Импортировать»**. Фоновый процесс сразу начнёт тестирование.
3. **Создание подключения:**
   Перейдите в раздел **«Клиенты»**, введите имя устройства (например, `phone`) и нажмите **«Создать»**. Отсканируйте появившийся QR-код камерой приложения WireGuard или скачайте `.conf`-файл.
4. **Проверка работы:**
   Активируйте туннель в приложении WireGuard. После подключения панель управления также доступна по внутреннему адресу `http://10.44.0.1/`.

---

## Архитектура и инженерные решения

```
                             [ Клиент WireGuard ]
                                      │
                         WireGuard UDP (порт 51820)
                                      ▼
  ┌─────────────────────────── Docker Host ───────────────────────────┐
  │                                                                   │
  │  ┌───────────────────────────────┐  ┌──────────────────────────┐  │
  │  │ Контейнер gateway             │  │ Контейнер controller     │  │
  │  │ (сетевой контур, NET_ADMIN)   │  │ (изолированный контур)   │  │
  │  │                               │  │                          │  │
  │  │ • Интерфейс wg0               │  │ • SQLite (WAL)           │  │
  │  │ • nftables fwmark routing     │  │ • Tournament Engine      │  │
  │  │ • Main Xray (клиентский поток)│  │ • Scheduler & Reserves   │  │
  │  │ • Probe Xray (фоновые тесты)  │  │ • Web UI Portal          │  │
  │  │ • Active Xray (прогрев резерва│  │ • REST API               │  │
  │  │ • Tor / Lyrebird (мосты)      │  │                          │  │
  │  └───────────────▲───────────────┘  └─────────────▲────────────┘  │
  │                  │                                │               │
  │                  └────────── Unix Socket ─────────┘               │
  │                            (agent.sock)                           │
  └──────────────────────────────────┬────────────────────────────────┘
                                     │
                    Динамический выбор лучшего прокси
                                     ▼
                [ Прокси-серверы VLESS Reality / Tor ]
```

### Изолированные контуры (Gateway и Controller)
1. **Gateway (сетевой контур):**
   Обладает сетевыми привилегиями (`NET_ADMIN`), управляет WireGuard, nftables и экземплярами Xray/Tor. Полностью изолирован от базы данных пользователей и мастер-ключей шифрования.
2. **Controller (контур управления):**
   Запущен под непривилегированным пользователем (`UID 10001`) в изолированном пространстве имен с `read_only: true` файловой системой. Хранит базу SQLite и выполняет математику ранжирования.
3. Взаимодействие происходит исключительно через локальный Unix-сокет `/run/hydrat/agent.sock`.

### Тройной контур Xray для непрерывности работы
Чтобы регулярное тестирование тысяч узлов не влияло на пользовательский трафик, процессы разделены:
- **Main Xray:** обслуживает только реальный клиентский трафик. Никогда не перезагружается при проверках.
- **Probe Xray:** изолированный временный процесс для фонового турнирного тестирования кандидатов. Ротируется каждые 250 проверок для исключения утечек памяти.
- **Active Xray:** держит открытыми сокеты к резервным серверам для мгновенного бесшовного переключения при сбое.

---

## Эксплуатация и мониторинг

### Просмотр журналов

Шлюз ведёт структурированные журналы с автоматической ротацией и очисткой устаревших записей (хранятся до 72 часов в каталоге `${DATA_DIR:-./data}/logs`). Сервисы gateway и controller используют изолированные каталоги журналов. Для чтения сохранённых журналов требуются права root или sudo.

Просмотр последних 100 строк активных журналов:

```bash
sudo tail -n 100 "${DATA_DIR:-./data}/logs/gateway/gateway-current.log" \
  "${DATA_DIR:-./data}/logs/controller/controller-current.log"
```

Непрерывный просмотр журналов в реальном времени:

```bash
sudo tail -F "${DATA_DIR:-./data}/logs/gateway/gateway-current.log" \
  "${DATA_DIR:-./data}/logs/controller/controller-current.log"
```

### Проверка состояния системы (Health & Readiness)

- **Liveness probe (доступность сервиса):**
  ```bash
  curl -s http://127.0.0.1:8088/api/health
  # {"status":"ok"}
  ```
- **Readiness probe (готовность маршрутов и резервов):**
  ```bash
  curl -s http://127.0.0.1:8088/api/ready
  # {"status":"ok"}
  ```

---

## Безопасность

- **Никаких дефолтных секретов:** в репозитории отсутствуют предустановленные ключи или пароли. Всё генерируется криптографически стойкими методами при первом запуске.
- **Минимальные права доступа:** конфигурации с приватными ключами сохраняются с маской `0600`. Контейнер контроллера защищён флагами `no-new-privileges: true` и `read_only: true`.
- Подробные инструкции по созданию бэкапов и восстановлению доступны в документе [`docs/operations.md`](docs/operations.md).
- **Политика безопасности:** подробная информация об архитектурной защите и ответственном раскрытии уязвимостей представлена в [`SECURITY.md`](SECURITY.md).

---

## Участие в разработке (Contributing)

Мы приветствуем вклад сообщества в развитие проекта! Ознакомьтесь с руководством по разработке в [`CONTRIBUTING.md`](CONTRIBUTING.md) и кодексом поведения в [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md).

---

## Лицензия

Распространяется под свободной лицензией MIT. Подробнее см. файл [`LICENSE`](LICENSE).
